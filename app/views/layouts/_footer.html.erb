<% if parent_signed_in? %>
    <div class="clear"></div>
    <footer class='container-fluid bg-light bg-gradient text-warning text-end shadow-sm border-top'> Welcome <%= current_parent.name %>! </footer>
<% else %>
    <div class="clear"></div>
    <footer class='container-fluid bg-light bg-gradient text-warning text-end shadow-sm border-top'> A chores app for kids! </footer>
<% end %>